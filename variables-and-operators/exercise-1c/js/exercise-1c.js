/*
 Variables and Operators

Create two variables of String type and sum 
the length of each variable (number of letters of the string)
saving the result the sum a 3rd variable (use length).

 */
var strOne = "Two halves of the";
var strTwo = "same soul detached";
var strLengthSum = strOne.length + strTwo.length;
console.log("The sum of the lengths of both strings is " + strLengthSum + " characters");