/*
 Create a string variable with at least 10 characters and  generate a new string
  with the first 5 characters by saving the result to a new variable (use substring).
 */
var str = "Honesty is the first chapter in the book of wisdom";
var subString = str.substring(0, 5);
console.log(subString);